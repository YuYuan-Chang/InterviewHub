apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: interviewhub
  labels: { app: user-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: user-service }
  template:
    metadata:
      labels: { app: user-service }
      annotations:
        # hook for a cluster Prometheus (kube-prometheus-stack et al.)
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4002'
        prometheus.io/path: /metrics
    spec:
      # migrations run once per pod before the app starts; migrate deploy holds
      # an advisory lock so concurrent replicas cannot race
      initContainers:
        - name: migrate
          image: interviewhub/user-service:dev
          imagePullPolicy: IfNotPresent
          command: ["sh", "-c", "until npx prisma migrate deploy --schema services/user/prisma/schema.prisma; do echo 'db not ready, retrying'; sleep 3; done"]
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef: { name: interviewhub-secrets, key: USER_DATABASE_URL }
      containers:
        - name: user-service
          image: interviewhub/user-service:dev
          imagePullPolicy: IfNotPresent
          command: ["node", "services/user/dist/index.js"]
          # let endpoint removal propagate before SIGTERM (zero-downtime rollouts)
          lifecycle:
            preStop:
              exec: { command: ["sh", "-c", "sleep 5"] }
          ports:
            - containerPort: 4002
          envFrom:
            - configMapRef: { name: interviewhub-config }
          env:
            - name: PORT
              value: "4002"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef: { name: interviewhub-secrets, key: USER_DATABASE_URL }
            - name: JWT_PUBLIC_KEY_B64
              valueFrom:
                secretKeyRef: { name: interviewhub-secrets, key: JWT_PUBLIC_KEY_B64 }
            - name: INTERNAL_TOKEN
              valueFrom:
                secretKeyRef: { name: interviewhub-secrets, key: INTERNAL_TOKEN }
          # readiness gates traffic on DB reachability; liveness only restarts a dead process
          readinessProbe:
            httpGet: { path: /healthz, port: 4002 }
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet: { path: /livez, port: 4002 }
            initialDelaySeconds: 15
            periodSeconds: 20
          # requests are required for the HPA's CPU utilization math
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits: { cpu: 500m, memory: 384Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: interviewhub
spec:
  selector: { app: user-service }
  ports:
    - port: 4002
      targetPort: 4002
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: user-service
  namespace: interviewhub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: user-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
