{
  "name": "@interviewhub/post-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch --env-file=../../infra/.env --env-file=.env src/index.ts",
    "prisma:generate": "prisma generate",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@interviewhub/shared": "*",
    "@prisma/client": "^6.10.0",
    "express": "^5.1.0",
    "prisma": "^6.10.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  }
}
